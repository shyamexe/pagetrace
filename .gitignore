node_modules
dist
coverage
*.log
CLAUDE.md

# Built from src/browser.ts by `npm run build:site`; the Pages workflow rebuilds it.
# Its own directory: tsup cleans its out-dir, and docs/ holds hand-written HTML.
docs/assets/
